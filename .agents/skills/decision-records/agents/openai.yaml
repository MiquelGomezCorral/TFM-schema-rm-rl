interface:
  display_name: "Planning · Decision Records"
  short_description: "Preserve important implementation decisions"
  default_prompt: "Use $decision-records to assess, create, update, or reconcile durable decisions for this implementation."

policy:
  allow_implicit_invocation: true
