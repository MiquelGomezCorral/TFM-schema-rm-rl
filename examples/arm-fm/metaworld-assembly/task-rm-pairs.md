# Task–RM Pairs

## Paper-associated pair

- Task: `MW-ASSEMBLY-1` in `tasks.md`.
- Reward Machine: generic `MW-RM-1` in `reward-machines.md`.
- Pair status: paper-associated, not an exact released generator pair.

Event mapping: `near_object` → `near_nut`, `grasp_success` → `nut_grasped`,
`object_near_goal` → `nut_near_peg`, and `success` → `assembly_complete`.
