# Task–RM Pairs

## Paper-associated pair

- Task: `MW-SHELF-1` in `tasks.md`.
- Reward Machine: generic `MW-RM-1` in `reward-machines.md`.
- Pair status: paper-associated, not an exact released generator pair.

Event mapping: `near_object` → `near_object`, `grasp_success` → `object_grasped`,
`object_near_goal` → `object_near_shelf_goal`, and `success` → `object_on_shelf`.
